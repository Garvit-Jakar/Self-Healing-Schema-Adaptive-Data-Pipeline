# Self-Healing-Schema-Adaptive-Data-Pipeline
Build an event-driven Python pipeline that uses an LLM agent to detect schema drift, write its own patch code, test the fix in a sandbox, and dynamically update the pipeline without human intervention.
